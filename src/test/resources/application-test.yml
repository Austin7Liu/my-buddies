spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:my_buddies_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false

app:
  jwt:
    secret: my-buddies-test-secret-at-least-32-bytes
