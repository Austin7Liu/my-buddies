spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:my_partner_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false
